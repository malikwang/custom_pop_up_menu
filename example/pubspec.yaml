name: example
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  custom_pop_up_menu:
    path: '../'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

